name: Scrape Table Sums

on:
  push:
  workflow_dispatch:

jobs:
  scrape:
    runs-on: ubuntu-latest

    steps:
      - name: Step 24f1002702@ds.study.iitm.ac.in
        run: echo "Starting scraping job"

      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install dependencies
        run: |
          npm ci || npm install
          npx playwright install chromium --with-deps

      - name: Run scraper
        run: node scrape.js
